/**
 * @template T
 * @typedef {object} TypedEvent
 * @property {T | null} target Returns the object to which event is dispatched (its target).
 * @property {T | null} currentTarget Returns the object whose event listener's callback is currently being invoked.
 * @property {T | null} srcElement
 */
